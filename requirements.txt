# ==========================================================
# Direct dependencies
# ==========================================================

pytest==9.1.1
requests==2.34.2  # app/llm_client.py -- calls a locally running Ollama server for tier 3

# ==========================================================
# Not yet added, deliberately:
# - Anything for the order-ledger/bank-statement parsers -- waiting on
#   real export samples before knowing what's actually needed to read them
#   (see app/parsers/order_ledger.py and app/parsers/bank_statement.py).
# ==========================================================
